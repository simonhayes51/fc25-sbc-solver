{
  "name": "fc25-sbc-solver",
  "version": "2.0.0",
  "description": "FC25 SBC Solution Finder with enhanced multi-source data collection",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "test-scraper": "node -e \"const Scraper = require('./src/live-sbc-scraper'); const s = new Scraper(); s.getActiveSBCs().then(r => console.log('Result:', r)).catch(e => console.error('Error:', e));\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-cron": "^3.0.3",
    "lodash": "^4.17.21",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
